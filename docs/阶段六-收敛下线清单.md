# 阶段六：收敛下线清单

> ⚠️ 执行前提：SPA 已逐模块完全替换视图层，双轨期各模块已切换且回归通过。**当前 SPA 仅为骨架 + 列表页，尚未达到执行条件**，本文档是收敛期的执行清单，供届时使用。

---

## 一、删除对象

### 1. 依赖（pom）
| 位置 | 删除项 |
|---|---|
| `datagear-webapp/pom.xml` | `spring-boot-starter-freemarker` |
| `datagear-webapp/.../DataGearApplication.java` | `FreeMarkerAutoConfiguration` 排除项清理 |

### 2. web 层视图四件套（datagear-web）
| 文件 | 说明 |
|---|---|
| `config/WebMvcConfigurerConfigSupport.java` 中视图解析器/`FreeMarkerConfigurer` 注册 | 删除视图相关 bean |
| `freemarker/CustomFreeMarkerView.java` | 删除 |
| `freemarker/WriteJsonTemplateDirectiveModel.java` | 删除 |
| `freemarker/` 包 | 删除 |

### 3. 模板与静态资源（datagear-web/src/main/resources）
| 路径 | 说明 |
|---|---|
| `org/datagear/web/templates/` | 136 个 .ftl |
| `static/lib/` | vue 3.2.47 / primevue 3.34.1 / jquery / codemirror 5.64 / echarts 5.6 / clipboard / marked |
| `static/script/pages/include/page.js` | po 框架 146KB |
| `static/script/util.js` | jQuery 工具集 87KB |
| `static/theme/` 覆盖层 CSS | 建议迁入前端工程（或删除） |

### 4. 前端构建产物拷贝
| 项 | 说明 |
|---|---|
| `datagear-web-ui/dist/` | `npm run build` 产物拷入静态资源目录（`static/` 或 Spring 托管） |

---

## 二、保留对象（冻结面，勿删）

- `datagear-analysis` 的 `freemarker` 依赖与 `datasettpl` 包（数据集模板语言）
- `static/analysisapi/`（1.0/2.0）、`static/analysislib/`（echarts/geojson 7.8MB）、builtInChartPlugins（150 zip）
- `/dv/**`、`/cv/**`、3 个 Compat 控制器、`/checkCode/**`
- `;jsessionid=` 支持、frameOptions 禁用

---

## 三、收敛后冒烟清单（M5）

1. iframe 嵌入图表（`/cv/{id}/`）、看板（`/dv/{id}/`）
2. 看板密码分享访问、匿名访问（数据库装配默认角色权限语义）
3. 3 个 Compat 控制器旧公开 URL
4. 图表插件运行时 JS 注入与展示
5. `/dv/heartbeat` 心跳轮询不报错
6. 可执行 war（`java -jar`）与外置 Tomcat 双形态启动验证
7. 首屏 < 300KB gzip（路由级代码分割生效）

---

## 四、删除统计（供核查）

| 项 | 数量 |
|---|---|
| .ftl 模板 | 136 |
| po 框架 | page.js 146KB + util.js 87KB ≈ 234KB |
| vendored lib | static/lib/ 下 10 个库 |
| FreeMarker 视图四件套 | 4 |

> 删除时建议：先 `git mv` 到 `deprecated/` 目录保留一个迭代，确认无回归后再物理删除。
