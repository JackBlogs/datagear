# DataGear 前后端分离改造 · 进度报告

> 更新：阶段二后端 API 化（主体）+ 阶段三模范模块前端接线完成。
> 环境：后端 50401（`disableLoginCheckCode=true` 命令行参数）、前端 5173（Vite）。

---

## 一、已完成

### 阶段零：契约盘点
- `docs/端点契约盘点表.md`（32 控制器 ~230 端点）
- `docs/不迁移清单-冻结面.md`
- `docs/api.md`（统一响应体/分页/错误码/401/meta 契约）

### 阶段一：前端骨架
- `datagear-web-ui/`（Vite 4 + Vue 3.2.47 + TS 5 + Pinia 2 + PrimeVue 3.34 + vue-router 4 + vue-i18n + axios）
- 登录 POC 跑通（Session 登录链路 + 302→JSON + 校验码 + 401）

### 阶段二：后端 API 化
1. **`OperationMessage<T>` 泛型化**（类 + 工厂方法；`AbstractController` 辅助方法保持 raw 向后兼容）。
2. **`/api` 前缀端点铺开**（双轨，旧 `/xxx` 端点不动）：

| 基类 | 模块 | 端点 |
|---|---|---|
| `AbstractEntityApiController<T>` | role, user, dtbsSourceGuard | `POST /api/{m}/pagingQueryData`、`GET /api/{m}/meta` |
| `AbstractDataPermissionApiController<T>` | dtbsSource, fileSource, analysisProject, dataSet, chart, dashboard | 同上 |
| （独立） | auth | `GET /api/auth/me` |

3. **安全配置**：`SecurityConfigSupport` 各 `xxxModuleAccess()` 扩展 `/api/{module}/**`，权限与旧端点一致。

### 阶段三：模范模块前端接线
- `src/stores/auth.ts` `fetchMe()` ↔ `/api/auth/me`；`permissions.ts` 存 `ModulePermissions`。
- `src/api/role.ts`、`src/api/user.ts`；`RoleListView.vue`、`UserListView.vue`（DataTable 懒加载 ↔ PagingData）。
- 路由 `/role`、`/user` + 侧菜单 + 登录后 `fetchMe()`。
- `npm run build` 通过。

### 阶段四：批量迁移（前端列表页）
- `usePagingTable()`（P0）、`useSearchForm()`/`useConfirm()`（P1）、`useSteps()`（P2）组合函数。
- `PagingTable.vue` 泛型组件 + `createPagingLoader()` + `modules.ts` 配置 + `ModuleListView.vue`。
- 7 模块列表页一键铺开（dtbsSource/fileSource/analysisProject/dataSet/chart/dashboard/dtbsSourceGuard）+ 路由 + 菜单。
- 排序字段 `orders:[{name,type}]` 与后端对齐并验证通过。

### 阶段五：复杂模块
**5b SQL 工作台 —— 完整 Vue 实现**
- `CodeEditor.vue`（CodeMirror 5）、`SqlpadEditor.vue`（执行 + 消息分类渲染 + 结果网格 + SQL 历史 + 结果分页）、`api/sqlpad.ts`。

**5c 看板设计器「数据绑定」+「样式」+「交互」分组 —— 完整 Vue 重写**
- `ChartPropertyPanel.vue`（属性面板容器：图表类型/数据绑定/样式/交互 四 Tab）
- `DataSetBindForm.vue`（编排：数据集列表 + 按需加载字段）、`DataSetBindPanel.vue`（绑定 UI）、`ChartStylePanel.vue`（样式）、`ChartInteractionPanel.vue`（交互）
- 后端新增：`/api/chartPlugin/list`、`/api/chartPlugin/dataSigns/{id}`、`/api/dataSet/getProfileDataSetByIds`、`/api/dataSet/resolveSql`
- 类型：`types/dashboard.ts`（DataSign/DataSet/DataSetBind/ChartStyle/ChartInteraction/ChartModel）

**dataSet SQL 表单（范式 C 模范模块）**
- `DataSetSqlForm.vue`（名称 + 数据源 + SQL 编辑器 + 预览解析 + 保存）

**5a 数据交换 —— 导出 + 导入向导 Vue 重写**
- `DataExchangeExportView.vue`（选表 → 选格式 → 导出 → 轮询进度 → 下载 ZIP）
- `DataExchangeImportView.vue`（上传文件 → 表名 → 导入 → 轮询进度）
- `api/dataExchange.ts`（getAllTableNames/uploadImportFile/doImport/doExport/message/download）

**认证守卫**：路由全局守卫（未认证跳登录 + `redirectUrl` 回跳），登录后 `fetchMe()`。

---

## 二、验证结果

- `POST /login/doLogin` → 302 → `/login/success`（JSON，含 `data.redirectUrl`）✅
- `GET /api/auth/me` → user/roles/modulePermissions/readonlyAction ✅
- 9 模块 `/api/{m}/pagingQueryData` 全部 HTTP 200（`OperationMessage<PagingData<T>>`）✅
- 未认证 + `X-Requested-With: XMLHttpRequest` → 401（`AjaxAwareAuthenticationEntryPoint`）✅

---

## 三、剩余待办（按阶段）

### 阶段二收尾（低风险）
1. **401 响应体 JSON 化**：当前 401 状态正确但 body 是 HTML（`sendError` 触发 error.ftl）。前端只判 status 已可用；如需 body JSON，改 `AjaxAwareAuthenticationEntryPoint.commenceForAjax` 直写 JSON。
2. **裸返回端点收敛**（§五决议清单）：`queryData`（List）、`hasReadPermission`（boolean[]）、`uploadImportFile`、`getLogContent`（HTML）等——多为阶段四/五模块，随模块迁移时处理。
3. **特殊模块**：`authorization`（资源维度 `/{resourceType}/{resource}/queryData`，需独立设计）、`chartPlugin`/`dashboardGlobalRes`（文件型 manager 非 EntityService）、`driverEntity`（connection 层 `DriverEntityManager`）。

### 阶段四：批量迁移（前端页面）
- 以 `RoleListView`/`UserListView` 为模板，铺开其余模块列表页；
- 沉淀 `usePagingTable()` / `useSearchForm()` 组合函数（替代 `setupAjaxTable`）；
- 排序字段与后端 `PagingQuery` 对齐。

### 阶段五：复杂模块（见 `docs/阶段五-复杂模块包装迁移方案.md`）
- 5a 数据交换 + URL 构建器；5b SQL 工作台 + dtbsSourceData；5c 看板设计器（包装迁移优先）。

### 阶段六：收敛下线
- 删除 web 层 FreeMarker 视图体系（`spring-boot-starter-freemarker`、四件套、136 个 .ftl、static/lib、po 框架）；
- 嵌入方冒烟（iframe/密码分享/匿名/jsessionid）；双形态打包验证。

---

## 四、关键约定（勿破坏）

- **双轨**：旧 `/xxx` 端点（裸返回）与新 `/api/xxx`（`OperationMessage<T>`）并存，模块切换后旧模板保留一个迭代再删。
- **冻结面**：`/cv`、`/dv`、`/vres`、`/checkCode`、3 个 Compat 控制器、analysisapi/analysislib、图表插件运行时、数据集模板语言（freemarker）。
- **后端 `disableLoginCheckCode`**：仅命令行参数传入，`application.properties` 仍为 `false`；正式启动需去掉该参数恢复校验码。
