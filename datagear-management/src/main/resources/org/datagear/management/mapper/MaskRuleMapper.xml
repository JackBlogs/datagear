<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.datagear.management.domain.MaskRuleEntity">

	<insert id="insert">
		INSERT INTO DATAGEAR_MASK_RULE
			(
			MK_ID, MK_FIELD, MK_TABLE_NAME, MK_FIELD_NAME, MK_ALGO,
			MK_SAMPLE, MK_ROLES, MK_ENABLED, MK_CREATE_TIME, MK_CREATE_USER_ID
			)
		VALUES
			(
			#{entity.id}, #{entity.field}, #{entity.tableName}, #{entity.fieldName}, #{entity.algo},
			#{entity.sample}, #{entity.roles}, #{entity.enabled}, #{entity.createTime}, #{entity.createUserId}
			)
	</insert>

	<update id="update">
		UPDATE DATAGEAR_MASK_RULE SET
			MK_FIELD = #{entity.field},
			MK_TABLE_NAME = #{entity.tableName},
			MK_FIELD_NAME = #{entity.fieldName},
			MK_ALGO = #{entity.algo},
			MK_SAMPLE = #{entity.sample},
			MK_ROLES = #{entity.roles},
			MK_ENABLED = #{entity.enabled}
		WHERE
			MK_ID = #{entity.id}
	</update>

	<delete id="deleteById">
		DELETE FROM DATAGEAR_MASK_RULE
		WHERE
			MK_ID = #{id}
	</delete>

	<select id="getById" resultType="org.datagear.management.domain.MaskRuleEntity">
		SELECT
			T.*
		FROM
			(<include refid="queryView" />) T
		WHERE
			T.${_iq_}id${_iq_} = #{id}
	</select>

	<select id="query" resultType="org.datagear.management.domain.MaskRuleEntity">
		SELECT
			T.*
		FROM
			(<include refid="queryView" />) T
		WHERE
			<include refid="queryCondition" />
		<include refid="common.queryOrder" />
	</select>

	<select id="pagingQueryCount" resultType="int">
		SELECT
			COUNT(*)
		FROM
			(<include refid="queryView" />) T
		WHERE
			<include refid="queryCondition" />
	</select>

	<select id="pagingQuery" resultType="org.datagear.management.domain.MaskRuleEntity">
		<include refid="common.pagingQueryHead" />
			SELECT
				T.*
			FROM
				(<include refid="queryView" />) T
			WHERE
				<include refid="queryCondition" />
			<include refid="common.queryOrder" />
		<include refid="common.pagingQueryFoot" />
	</select>

	<sql id="queryView">
		SELECT
			A.MK_ID AS ${_iq_}id${_iq_},
			A.MK_FIELD AS ${_iq_}field${_iq_},
			A.MK_TABLE_NAME AS ${_iq_}tableName${_iq_},
			A.MK_FIELD_NAME AS ${_iq_}fieldName${_iq_},
			A.MK_ALGO AS ${_iq_}algo${_iq_},
			A.MK_SAMPLE AS ${_iq_}sample${_iq_},
			A.MK_ROLES AS ${_iq_}roles${_iq_},
			A.MK_ENABLED AS ${_iq_}enabled${_iq_},
			A.MK_CREATE_TIME AS ${_iq_}createTime${_iq_},
			A.MK_CREATE_USER_ID AS ${_iq_}createUserId${_iq_}
		FROM
			DATAGEAR_MASK_RULE A
	</sql>

	<sql id="queryCondition">
		1 = 1
		<if test="queryKeyword != null">
		AND
		(
			${_iq_}field${_iq_} LIKE #{queryKeyword} OR
			${_iq_}algo${_iq_} LIKE #{queryKeyword}
		)
		</if>
	</sql>

	<select id="listAll" resultType="org.datagear.management.domain.MaskRuleEntity">
		SELECT
			T.*
		FROM
			(<include refid="queryView" />) T
		ORDER BY
			T.${_iq_}createTime${_iq_} DESC
	</select>

	<select id="listEnabledByTable" resultType="org.datagear.management.domain.MaskRuleEntity">
		SELECT
			T.*
		FROM
			(<include refid="queryView" />) T
		WHERE
			T.${_iq_}enabled${_iq_} = 1
			AND T.${_iq_}tableName${_iq_} = #{tableName}
	</select>
</mapper>
