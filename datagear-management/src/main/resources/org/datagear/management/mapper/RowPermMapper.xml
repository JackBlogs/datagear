<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.datagear.management.domain.RowPermEntity">

	<insert id="insert">
		INSERT INTO DATAGEAR_ROW_PERM
			(
			RP_ID, RP_NAME, RP_RESOURCE, RP_COND, RP_ROLES,
			RP_TABLE_NAME, RP_FIELD_NAME, RP_ENABLED,
			RP_CREATE_TIME, RP_CREATE_USER_ID
			)
		VALUES
			(
			#{entity.id}, #{entity.name}, #{entity.resource}, #{entity.cond}, #{entity.rolesJson},
			#{entity.tableName}, #{entity.fieldName}, #{entity.enabled},
			#{entity.createTime}, #{entity.createUserId}
			)
	</insert>

	<update id="update">
		UPDATE DATAGEAR_ROW_PERM SET
			RP_NAME = #{entity.name},
			RP_RESOURCE = #{entity.resource},
			RP_COND = #{entity.cond},
			RP_ROLES = #{entity.rolesJson},
			RP_TABLE_NAME = #{entity.tableName},
			RP_FIELD_NAME = #{entity.fieldName},
			RP_ENABLED = #{entity.enabled}
		WHERE
			RP_ID = #{entity.id}
	</update>

	<delete id="deleteById">
		DELETE FROM DATAGEAR_ROW_PERM
		WHERE
			RP_ID = #{id}
	</delete>

	<select id="getById" resultType="org.datagear.management.domain.RowPermEntity">
		SELECT
			T.*
		FROM
			(<include refid="queryView" />) T
		WHERE
			T.${_iq_}id${_iq_} = #{id}
	</select>

	<select id="query" resultType="org.datagear.management.domain.RowPermEntity">
		SELECT
			T.*
		FROM
			(<include refid="queryView" />) T
		WHERE
			<include refid="queryCondition" />
		<include refid="common.queryOrder" />
	</select>

	<select id="pagingQueryCount" resultType="int">
		SELECT
			COUNT(*)
		FROM
			(<include refid="queryView" />) T
		WHERE
			<include refid="queryCondition" />
	</select>

	<select id="pagingQuery" resultType="org.datagear.management.domain.RowPermEntity">
		<include refid="common.pagingQueryHead" />
			SELECT
				T.*
			FROM
				(<include refid="queryView" />) T
			WHERE
				<include refid="queryCondition" />
			<include refid="common.queryOrder" />
		<include refid="common.pagingQueryFoot" />
	</select>

	<sql id="queryView">
		SELECT
			A.RP_ID AS ${_iq_}id${_iq_},
			A.RP_NAME AS ${_iq_}name${_iq_},
			A.RP_RESOURCE AS ${_iq_}resource${_iq_},
			A.RP_COND AS ${_iq_}cond${_iq_},
			A.RP_ROLES AS ${_iq_}rolesJson${_iq_},
			A.RP_TABLE_NAME AS ${_iq_}tableName${_iq_},
			A.RP_FIELD_NAME AS ${_iq_}fieldName${_iq_},
			A.RP_ENABLED AS ${_iq_}enabled${_iq_},
			A.RP_CREATE_TIME AS ${_iq_}createTime${_iq_},
			A.RP_CREATE_USER_ID AS ${_iq_}createUserId${_iq_}
		FROM
			DATAGEAR_ROW_PERM A
	</sql>

	<sql id="queryCondition">
		1 = 1
		<if test="queryKeyword != null">
		AND
		(
			${_iq_}name${_iq_} LIKE #{queryKeyword} OR
			${_iq_}resource${_iq_} LIKE #{queryKeyword}
		)
		</if>
	</sql>

	<select id="listAll" resultType="org.datagear.management.domain.RowPermEntity">
		SELECT
			T.*
		FROM
			(<include refid="queryView" />) T
		ORDER BY
			T.${_iq_}createTime${_iq_} DESC
	</select>

	<select id="listEnabledByTable" resultType="org.datagear.management.domain.RowPermEntity">
		SELECT
			T.*
		FROM
			(<include refid="queryView" />) T
		WHERE
			T.${_iq_}enabled${_iq_} = 1
			AND T.${_iq_}tableName${_iq_} = #{tableName}
	</select>
</mapper>
