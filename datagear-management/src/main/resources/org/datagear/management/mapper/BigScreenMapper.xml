<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.datagear.management.domain.BigScreenEntity">

	<insert id="insert">
		INSERT INTO DATAGEAR_BIG_SCREEN
			(
			BS_ID, BS_NAME, BS_DESCRIPTION, BS_THEME, BS_DESIGN_JSON, BS_CREATE_TIME
			)
		VALUES
			(
			#{entity.id}, #{entity.name}, #{entity.description}, #{entity.theme}, #{entity.designJson}, #{entity.createTime}
			)
	</insert>

	<update id="update">
		UPDATE DATAGEAR_BIG_SCREEN SET
			BS_NAME = #{entity.name},
			BS_DESCRIPTION = #{entity.description},
			BS_THEME = #{entity.theme},
			BS_DESIGN_JSON = #{entity.designJson}
		WHERE
			BS_ID = #{entity.id}
	</update>

	<delete id="deleteById">
		DELETE FROM DATAGEAR_BIG_SCREEN
		WHERE
			BS_ID = #{id}
	</delete>

	<select id="getById" resultType="org.datagear.management.domain.BigScreenEntity">
		SELECT
			T.*
		FROM
			(<include refid="queryViewWithJson" />) T
		WHERE
			T.${_iq_}id${_iq_} = #{id}
	</select>

	<select id="query" resultType="org.datagear.management.domain.BigScreenEntity">
		SELECT
			T.*
		FROM
			(<include refid="queryView" />) T
		WHERE
			<include refid="queryCondition" />
		<include refid="common.queryOrder" />
	</select>

	<select id="pagingQueryCount" resultType="int">
		SELECT
			COUNT(*)
		FROM
			(<include refid="queryView" />) T
		WHERE
			<include refid="queryCondition" />
	</select>

	<select id="pagingQuery" resultType="org.datagear.management.domain.BigScreenEntity">
		<include refid="common.pagingQueryHead" />
			SELECT
				T.*
			FROM
				(<include refid="queryView" />) T
			WHERE
				<include refid="queryCondition" />
			<include refid="common.queryOrder" />
		<include refid="common.pagingQueryFoot" />
	</select>

	<sql id="queryView">
		SELECT
			A.BS_ID AS ${_iq_}id${_iq_},
			A.BS_NAME AS ${_iq_}name${_iq_},
			A.BS_DESCRIPTION AS ${_iq_}description${_iq_},
			A.BS_THEME AS ${_iq_}theme${_iq_},
			A.BS_CREATE_TIME AS ${_iq_}createTime${_iq_}
		FROM
			DATAGEAR_BIG_SCREEN A
	</sql>

	<sql id="queryViewWithJson">
		SELECT
			A.BS_ID AS ${_iq_}id${_iq_},
			A.BS_NAME AS ${_iq_}name${_iq_},
			A.BS_DESCRIPTION AS ${_iq_}description${_iq_},
			A.BS_THEME AS ${_iq_}theme${_iq_},
			A.BS_DESIGN_JSON AS ${_iq_}designJson${_iq_},
			A.BS_CREATE_TIME AS ${_iq_}createTime${_iq_}
		FROM
			DATAGEAR_BIG_SCREEN A
	</sql>

	<sql id="queryCondition">
		1 = 1
		<if test="queryKeyword != null">
		AND
		(
			${_iq_}name${_iq_} LIKE #{queryKeyword} OR
			${_iq_}description${_iq_} LIKE #{queryKeyword}
		)
		</if>
	</sql>

</mapper>
