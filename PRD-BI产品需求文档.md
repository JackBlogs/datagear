# DataGear 商业智能分析平台产品需求文档（PRD）

> 本文档在 DataGear 6.0.0 既有产品能力基础上，融合国内主流 BI 软件的产品理念与功能形态，面向「功能丰富的 BI 软件」目标进行系统性完善，用于指导后续产品的设计、开发、测试与实施。

---

## 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | DataGear（数据可视化分析与商业智能平台） |
| 当前版本 | 6.0.0（规划升级至 BI 完整版） |
| 文档类型 | 产品需求文档（PRD） |
| 文档目的 | 在既有能力上补充数据治理、GIS/三维/大屏、数据下钻、统计报表、系统管理、数据交换与分享、综合首页等模块，形成完整 BI 产品需求基线 |
| 适用范围 | 产品、设计、开发、测试、实施及二次开发团队 |
| 许可协议 | LGPL-3.0（开源版）；另提供企业版 |
| 参考产品 | 帆软 FineBI/FineReport、永洪 Yonghong BI、思迈特 Smartbi、亿信 ABI、阿里 Quick BI、百度 Sugar、观远数据、网易有数、腾讯云 BI |

---

## 产品概述

### 产品定位

DataGear 是一款**开源免费的商业智能（BI）分析平台**，核心价值主张从「自由制作任何数据看板」升级为「**一站式数据接入、治理、建模、分析与展示平台**」。产品在保持私有化单体部署、轻量开箱即用的同时，补齐数据治理、数据存储、数据下钻、统计报表、系统管理与数据交换分享能力，覆盖「接入 → 存储 → 治理 → 建模 → 分析 → 展示 → 分享」完整数据链路。

### 产品背景与要解决的问题

| 痛点 | DataGear 的解法 |
| --- | --- |
| 数据分散在数据库、文件、接口、消息队列等，缺乏统一入口 | 统一数据源管理，支持 JDBC 数据库、Excel/CSV/JSON 文件、HTTP 接口、实时流等多类接入 |
| 数据质量参差、口径不一、缺乏治理 | 元数据管理、数据标准、数据质量、数据血缘、指标管理、数据脱敏构成数据治理体系 |
| 传统 BI 笨重、部署复杂、成本高 | 单体轻量架构，Java 8+ 即可运行，私有化掌控数据 |
| 图表类型有限、缺 GIS/三维/大屏能力 | 70+ 内置图表 + GIS 地图工具 + 三维立体图表 + 大数据可视化大屏 + 图表插件机制 |
| 分析深度不足，难以层层下钻 | 数据钻取、图表联动、跳转穿透、参数过滤构成自助交互分析体系 |
| 报表能力弱、无法满足中国式复杂报表 | 分组报表、交叉表、电子表格、填报、定时订阅推送与多格式导出 |
| 权限与数据安全难保障 | 基于角色的功能权限 + 资源/行列级数据权限 + 数据源防护 + 加密与脱敏 |
| 数据难以对外交换与分享 | 数据导入导出、数据 API 服务、看板/报表分享、订阅推送 |

### 目标用户与典型场景

| 角色 | 场景 |
| --- | --- |
| 数据分析师 | 连接数据源、建模、拖拽生成图表、搭建看板与报表、自助下钻分析 |
| 业务人员 | 查看门户首页、访问已分享看板/报表、通过交互表单与钻取自取所需数据 |
| 数据工程师 | 配置数据接入、执行数据治理、维护元数据与数据质量、开发数据交换任务 |
| 开发人员 | 自定义图表插件、扩展数据源驱动、调用数据 API、二次开发 |
| 系统/数据管理员 | 管理用户/角色/组织/数据权限、数据源防护、系统配置 |
| 企业决策层 | 大屏驾驶舱、经营报表、指标看板的集中展示与分享 |

### 产品价值

- **安全稳定**：私有化部署、数年迭代、基于角色的权限、数据默认私有、加密脱敏。
- **功能丰富**：多源接入、六类数据集、70+ 图表、GIS/三维/大屏、报表、模板化看板。
- **治理可控**：元数据、数据标准、数据质量、数据血缘、指标管理一体的轻量数据治理。
- **易于扩展**：运行时数据源驱动、自定义图表插件、开放看板 API 与数据 API。
- **自由制作**：原生 HTML 模板、可视化/源码编辑、多端响应式、联动钻取。

### 参考产品与竞品对标

对标国内主流 BI 软件，吸收其成熟能力并保持 DataGear 轻量开放优势：

| 竞品 | 核心优势 | DataGear 借鉴方向 |
| --- | --- | --- |
| 帆软 FineBI / FineReport | 中国式复杂报表、自助分析、大屏 | 补充分组/交叉/电子表格报表、填报、大屏 |
| 永洪 Yonghong BI | 敏捷自助分析、高性能计算 | 强化拖拽自助分析与数据集市 |
| 思迈特 Smartbi | 一站式（电子表格、数据挖掘、移动 BI、门户） | 补充数据门户与综合首页、移动端 |
| 亿信 ABI | 数据治理与 BI 一体化 | 引入元数据/数据质量/数据血缘/指标管理 |
| 阿里 Quick BI | 云端 BI、权限与分享 | 完善角色/行列级数据权限与分享 |
| 百度 Sugar | 低门槛可视化大屏 | 补充三维立体图表与大屏组件库 |
| 观远数据 | 指标管理与经营分析 | 引入指标管理与经营驾驶舱 |
| 网易有数 / 腾讯云 BI | 数据 API 与生态开放 | 补充数据 API 服务与订阅推送 |

---

## 总体功能架构

```
DataGear 商业智能分析平台
├── 综合首页（数据门户 / 工作台 / 全局搜索 / 收藏与最近访问）
├── 数据接入
│   ├── 数据源管理（数据库 / 文件 / HTTP 接口 / 实时流）
│   ├── 数据源驱动管理（运行时 JDBC 驱动加载）
│   ├── SQL 工作台（执行 / 历史 / 结果导出）
│   └── 数据源防护（URL / 连接属性防护规则）
├── 数据存储
│   ├── 系统元数据存储（内置 Derby / 外置 MySQL）
│   ├── 数据缓存（数据集缓存 / 连接池）
│   └── 数据抽取与物化存储（数据集市 / 数据模型）
├── 数据治理
│   ├── 元数据管理（库 / 表 / 字段字典）
│   ├── 数据标准（命名 / 字典 / 标准值域）
│   ├── 数据质量（规则 / 校验 / 质量报告）
│   ├── 数据血缘（数据集 / 指标 / 报表依赖追踪）
│   ├── 指标管理（指标定义 / 口径 / 统一消费）
│   └── 数据安全与脱敏（脱敏规则 / 敏感信息识别）
├── 数据处理与建模
│   ├── 数据集（SQL / HTTP / CSV / Excel / JSON / 文件）
│   ├── 数据集参数与参数化语法（Freemarker 模板）
│   └── 数据建模（多维模型 / 自助数据准备 / 组合数据集）
├── 数据分析与展示
│   ├── 图表（70+ 内置图表，一图多数据集）
│   ├── GIS 工具与地图（GeoJSON / 坐标 / 热力 / 轨迹）
│   ├── 三维立体图表（3D 柱 / 3D 地图 / 3D 散点 / 立体大屏）
│   ├── 大数据大屏（驾驶舱 / 数据大屏模板）
│   └── 移动端展示（多端响应式）
├── 数据下钻与交互分析
│   ├── 数据钻取（地图 / 层级 / 明细穿透）
│   ├── 图表联动 / 跳转 / 参数过滤
│   └── 交互表单 / 异步加载
├── 统计报表
│   ├── 报表设计（分组 / 交叉 / 电子表格 / 明细）
│   ├── 报表导出（Excel / PDF / Word / CSV）
│   └── 定时报表与订阅推送
├── 系统管理
│   ├── 用户 / 组织 / 角色管理
│   ├── 功能权限 + 数据权限（资源 / 行列级）
│   └── 登录安全 / 系统设置
└── 数据交换及分享
    ├── 数据导入导出（CSV / Excel / JSON / SQL）
    ├── 数据 API 服务（对外发布数据集 / 指标）
    └── 看板 / 报表分享与订阅推送
```

---

## 数据接入

### 功能概述

对可接入的外部数据进行统一注册、维护与访问控制，覆盖数据库、文件、HTTP 接口、实时流等多类来源，是平台的数据入口。

### 数据库接入

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-DS-01 | 新建/编辑数据源 | 标题、JDBC URL、用户名、密码、模式名（Schema）、连接属性、指定驱动 |
| FR-DS-02 | 删除/复制数据源 | 级联清理关联历史；复制快速生成同构数据源 |
| FR-DS-03 | 连接校验 | 保存前测试数据库连通性 |
| FR-DS-04 | 数据源列表/分页查询 | 支持关键字、创建用户过滤 |
| FR-DS-05 | 表结构浏览 | 读取库/模式/表/列元信息（依赖 datagear-meta） |
| FR-DS-06 | 表数据查看/编辑 | 分页查询、增删改（依赖 datagear-persistence） |
| FR-DS-07 | 数据导入/导出 | CSV/Excel/JSON/SQL 格式（依赖 datagear-dataexchange） |
| FR-DS-08 | 数据源防护 | 基于 URL/用户/连接属性的防护规则 |
| FR-DS-09 | 密码加密存储 | 支持 `{noop}`/`{std}` 前缀，可配置 AES 加密 |

**业务规则**：

1. 数据源默认仅创建者可见，通过授权共享给角色/用户/全部/匿名。
2. 数据源密码默认明文（可配置开启加密），采用可配置密钥+盐。
3. 表类型可区分「数据表（可查）/实体表（可读写）」。

### 文件数据接入（Excel/CSV/JSON）

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-FILE-01 | Excel 数据集 | 读取 xls/xlsx，指定 Sheet、表头行、行列表达式 |
| FR-FILE-02 | CSV 数据集 | 粘贴文本或读取文件，指定编码、分隔符、表头行 |
| FR-FILE-03 | JSON 数据集 | 粘贴 JSON 文本或读取 JSON 文件，支持 JSON 路径解析 |
| FR-FILE-04 | 目录文件数据集 | 读取目录内多文件，按文件名模式合并 |
| FR-FILE-05 | 文件源管理 | 上传/下载数据集文件、目录管理、文件源授权 |
| FR-FILE-06 | 文件数据预览 | 上传后预览字段与样例数据，自动识别类型 |

### API 与 HTTP 接口接入

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-HTTP-01 | HTTP 数据集 | URI、请求头、方法、请求体、响应 JSON 路径、附加属性 |
| FR-HTTP-02 | 认证配置 | 支持 Basic/Bearer Token/自定义头等鉴权 |
| FR-HTTP-03 | 分页与翻页 | 支持分页参数、翻页取值，自动拼接多次请求 |
| FR-HTTP-04 | 响应解析 | JSON/XML 响应解析，支持 JSONPath 提取数据路径 |

### 实时与流式数据接入（规划）

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-STREAM-01 | 实时数据源 | 接入 Kafka、MQTT 等消息流，支撑实时看板 |
| FR-STREAM-02 | 定时刷新 | 图表/看板按间隔自动刷新 |
| FR-STREAM-03 | WebSocket 推送 | 数据变更通过 WebSocket 推送至前端实时渲染 |

### 数据源驱动管理

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-DRIVER-01 | 驱动上传/新增 | 上传 JDBC 驱动 jar 或指定驱动类 |
| FR-DRIVER-02 | 驱动列表/启停/删除 | 驱动可启用/禁用 |
| FR-DRIVER-03 | 驱动信息解析 | 驱动类、显示名、版本、兼容 URL 模板 |
| FR-DRIVER-04 | 驱动加载 | 从指定目录（`driverRootDirectory`）动态类加载 |

**扩展点**：支持 MySQL、PostgreSQL、Oracle、SQL Server、Elasticsearch、ClickHouse、OceanBase、TiDB、人大金仓、达梦、海量等；实现 `DriverEntityManager` / `PathDriverFactory` 可扩展加载策略。

### SQL 工作台

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-SQLPAD-01 | SQL 编辑与执行 | 多语句执行、结果集展示 |
| FR-SQLPAD-02 | SQL 历史 | 记录执行过的 SQL，可回填复用 |
| FR-SQLPAD-03 | 结果导出 | 将查询结果导出为文件 |
| FR-SQLPAD-04 | SQL 防注入 | 按权限档位校验非法关键字 |
| FR-SQLPAD-05 | 执行提交与异步执行 | 支持提交式执行（`SqlpadExecutionSubmit`） |

### 数据源防护

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-GUARD-01 | 防护规则 CRUD | 名称、URL 模式、用户模式、连接属性模式、匹配模式、优先级、启停 |
| FR-GUARD-02 | URL/属性防护 | 按 URL 模式 + 连接属性模式判定「允许/拒绝」 |
| FR-GUARD-03 | 内置漏洞防护 | 如禁用 MySQL `allowLoadLocalInfile` 等任意文件读取漏洞 |

---

## 数据存储

### 功能概述

为平台自身元数据与业务分析数据提供统一存储与缓存能力，支撑数据接入、治理、建模与展示。

### 系统元数据存储

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-STORE-01 | 系统库 | 内置 Derby（开箱即用）或外置 MySQL（生产推荐） |
| FR-STORE-02 | 业务元数据 | 用户、角色、授权、数据源、数据集、图表、看板、报表等实体存储 |
| FR-STORE-03 | 版本化升级 | DDL 版本化自动升级（`DATAGEAR_VERSION`） |

### 数据缓存

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-STORE-04 | 数据集缓存 | Caffeine 缓存，可配置最大条目（默认 10000）与过期时间 |
| FR-STORE-05 | 连接池 | 数据库连接池复用，控制并发与连接上限 |
| FR-STORE-06 | 查询结果缓存 | 对热点查询结果缓存，降低数据库压力 |

### 数据抽取与物化存储（规划）

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-STORE-07 | 数据抽取任务 | 定时将源库数据抽取至数据集市/中间表 |
| FR-STORE-08 | 数据集市 | 面向分析主题的轻量集市模型，支撑多维分析 |
| FR-STORE-09 | 物化视图/中间表 | 复杂查询结果物化，加速看板/报表 |

---

## 数据治理

### 功能概述

参照亿信 ABI、观远等产品的数据治理能力，构建「元数据—数据标准—数据质量—数据血缘—指标管理—数据安全」轻量治理闭环，保障数据可用、可信、可控。

### 元数据管理

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-GOV-01 | 技术元数据 | 库/模式/表/列结构、类型、注释的自动采集与展示 |
| FR-GOV-02 | 业务元数据 | 字段业务含义、别名、标签、负责人维护 |
| FR-GOV-03 | 数据字典 | 统一维护字典项，供图表/报表选择与翻译 |
| FR-GOV-04 | 元数据检索 | 按表/字段/字典全局检索与定位 |

### 数据标准

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-GOV-05 | 命名规范 | 表/字段/指标命名规范定义与校验 |
| FR-GOV-06 | 标准值域 | 枚举、编码、单位等标准值域定义 |
| FR-GOV-07 | 标准映射 | 源字段与标准字段的映射关系维护 |

### 数据质量

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-GOV-08 | 质量规则 | 非空、唯一、范围、格式、引用完整性等规则配置 |
| FR-GOV-09 | 质量校验 | 定时或触发式执行校验任务 |
| FR-GOV-10 | 质量报告 | 质量评分、问题清单、趋势统计与告警 |

### 数据血缘

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-GOV-11 | 字段血缘 | 从数据源到数据集、图表、报表、指标的字段级血缘追踪 |
| FR-GOV-12 | 影响分析 | 变更某一数据对象时，分析下游受影响范围 |
| FR-GOV-13 | 血缘可视化 | 以图形式展示数据流转路径 |

### 指标管理

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-GOV-14 | 指标定义 | 指标名称、口径、计算公式、聚合方式、业务归属 |
| FR-GOV-15 | 指标分类 | 指标按主题/部门/层级分类管理 |
| FR-GOV-16 | 指标消费 | 指标统一供给图表、报表、看板，避免口径不一致 |

### 数据安全与脱敏

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-GOV-17 | 敏感信息识别 | 自动识别手机号、身份证、银行卡、邮箱等敏感字段 |
| FR-GOV-18 | 脱敏规则 | 掩码、截断、替换、哈希等脱敏策略，按角色生效 |
| FR-GOV-19 | 动态脱敏 | 查询与展示时按数据权限动态脱敏 |

---

## 数据处理与建模

### 功能概述

将数据源、文件、HTTP 接口等抽象为统一「数据集」，并提供建模能力，供图表/看板/报表消费。

### 数据集类型

| 类型 | 标识 | 说明 | 关键配置 |
| --- | --- | --- | --- |
| SQL 数据集 | `SQL` | 基于数据源执行 SQL 查询 | 数据源、SQL 语句、参数 |
| HTTP 数据集 | `HTTP` | 调用 HTTP 接口并解析响应 | URI、请求头、方法、请求体、响应 JSON 路径 |
| JSON 值数据集 | `JsonValue` | 直接粘贴 JSON 文本 | JSON 内容、数据 JSON 路径 |
| JSON 文件数据集 | `JsonFile` | 读取 JSON 文件 | 文件名、编码、数据 JSON 路径、文件源 |
| CSV 值数据集 | `CsvValue` | 直接粘贴 CSV 文本 | CSV 内容、表头行 |
| CSV 文件数据集 | `CsvFile` | 读取 CSV 文件 | 文件名、编码、表头行、文件源 |
| Excel 数据集 | `Excel` | 读取 Excel 文件 | 文件名、Sheet、表头行、行列表达式 |
| 目录文件数据集 | （文件目录） | 读取目录内多个文件 | 目录、文件名模式 |

### 数据集参数与参数化语法

- 参数类型：`string / boolean / integer / number / object`。
- 参数输入控件：`text / select / radio / checkbox / textarea / date / time / datetime`，`select` 等可携带 `inputPayload` 定义选项 JSON。
- **参数化语法（SQL/JSON/CSV 模板）**：基于 Freemarker，使用 `${参数名}` 引用参数；SQL 数据集额外提供 `${pc(参数名)}` 预编译函数，防止 SQL 注入。

### 数据建模与多维分析

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-MODEL-01 | 字段定义 | 字段类型、标签、默认值、表达式（计算字段）、全名（嵌套路径）、是否数组 |
| FR-MODEL-02 | 组合数据集 | 多个数据集关联/合并为新的数据集（规划） |
| FR-MODEL-03 | 统计数据集 | 内置聚合、分组、透视的统计数据集（规划） |
| FR-MODEL-04 | 多维模型 | 维度/度量定义，支撑多维分析与下钻 |
| FR-MODEL-05 | 自助数据准备 | 拖拽式字段选择、过滤、排序、去重、派生列 |

**扩展点**：`DataSet` 接口 + `AbstractDataSet`/`AbstractResolvableDataSet` 抽象类，可自定义全新数据集类型。

---

## 数据展示

### 功能概述

提供覆盖常规图表、GIS 地图、三维立体图表、大数据大屏、移动端的完整可视化能力，是平台的核心价值呈现。

### 图表库

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-CHART-01 | 图表新建/编辑/删除/复制 | 基于图表插件创建图表部件（ChartWidget） |
| FR-CHART-02 | 一图多数据集 | 单图表绑定多数据集，独立查询/别名/字段映射 |
| FR-CHART-03 | 数据标记（DataSign） | 数据集字段到图表语义（名称/数值/类别）的映射 |
| FR-CHART-04 | 附件数据集 | 绑定不作为渲染数据、仅扩展功能的附加数据集 |
| FR-CHART-05 | 图表选项/属性值 | 持久化图表个性化 option 与插件属性 |
| FR-CHART-06 | 更新间隔 | 支持定时刷新（实时图表） |
| FR-CHART-07 | 预览/展示 | 独立图表展示，可分享给匿名用户 |
| FR-CHART-08 | 图表类型覆盖 | 柱状、折线、饼、散点、雷达、漏斗、仪表盘、词云、桑基、树图、关系图、热力图、箱线、瀑布等 70+ 类型 |

### GIS 工具与地图

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-MAP-01 | 区域地图 | 行政区划着色/下钻，支持 GeoJSON 注册 |
| FR-MAP-02 | 散点/气泡地图 | 经纬度散点、数值气泡渲染 |
| FR-MAP-03 | 热力地图 | 基于坐标密度的热力图 |
| FR-MAP-04 | 轨迹地图 | 路径/轨迹数据动态展示 |
| FR-MAP-05 | 自定义地图 | 上传 GeoJSON 或通过 `registerMap` 注册自定义底图 |
| FR-MAP-06 | GIS 坐标转换 | 支持 GCJ-02/WGS-84/BD-09 坐标系转换 |
| FR-MAP-07 | 地图下钻 | 全国 → 省 → 市 → 区县层级钻取 |

### 三维立体图表

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-3D-01 | 3D 柱状图 | 三维立体柱状/条形渲染 |
| FR-3D-02 | 3D 散点图 | 三维散点分布 |
| FR-3D-03 | 3D 曲面/地形 | 三维曲面与地形数据可视化 |
| FR-3D-04 | 3D 地图 | 立体地图、建筑高度、区域拉升 |
| FR-3D-05 | 3D 关系/网络图 | 三维关系网络展示 |
| FR-3D-06 | 立体大屏 | 结合三维场景的驾驶舱大屏 |

### 大数据大屏

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-SCREEN-01 | 大屏看板 | 大屏模板化的数据看板（HTML 模板） |
| FR-SCREEN-02 | 大屏组件库 | 数字滚动、边框、标题、装饰等大屏组件 |
| FR-SCREEN-03 | 大屏适配 | 按分辨率等比缩放，适配多尺寸拼接屏 |
| FR-SCREEN-04 | 实时刷新 | 大屏数据定时/实时刷新 |
| FR-SCREEN-05 | 大屏模板 | 内置通用大屏模板，支持一键套用 |

### 移动端展示

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-MOBILE-01 | 响应式看板 | 手机/平板/桌面/大屏多端适配 |
| FR-MOBILE-02 | 移动图表 | 移动端友好的图表渲染与手势交互 |
| FR-MOBILE-03 | 移动报表 | 报表在移动端的自适应展示 |

### 图表插件机制

- **形态**：一个 zip 包，至少包含 `plugin.json`、`renderer.js`、`icon.png`。
- **plugin.json 关键字段**：`id`、`nameLabel`、`icons`、`dataSignSpec`、`dataSetRange`、`version`、`order`、`categoryInfos`、`author`、`apiVersion`、`platformVersion`。
- **renderer.js**：一个 IIFE，接收 `plugin` 对象，返回图表渲染对象。
- **放置位置**：`chartPluginRootDirectory`（默认 `${DATAGEAR_HOME}/chartPlugin`）。
- 支持重写/扩展内置插件、自定义图表选项。

---

## 数据下钻与交互分析

### 功能概述

提供从汇总到明细、从整体到局部的多层下钻与联动能力，支撑自助式交互分析。

### 数据钻取

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-DRILL-01 | 层级钻取 | 时间/地区/组织等层级由粗到细下钻 |
| FR-DRILL-02 | 地图钻取 | 全国→省→市→区县逐级钻取 |
| FR-DRILL-03 | 明细穿透 | 从汇总指标穿透到明细数据列表 |
| FR-DRILL-04 | 上卷 | 由细粒度上卷至汇总粒度 |

### 图表联动

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-LINK-01 | 图表联动 | 一个图表作为参数源联动其他图表 |
| FR-LINK-02 | 联动回退 | 联动触发后可回退还原 |
| FR-LINK-03 | 异步加载 | 联动图表异步加载，避免阻塞 |

### 跳转与穿透

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-NAV-01 | 图表跳转 | 点击图表元素跳转到其他看板/报表/URL |
| FR-NAV-02 | 参数传递 | 跳转时携带当前值作为目标参数 |
| FR-NAV-03 | 外部链接 | 跳转到第三方系统或页面 |

### 过滤与参数

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-FILTER-01 | 全局过滤器 | 看板级过滤条件，作用于多个图表 |
| FR-FILTER-02 | 交互表单 | 看板内置表单，参数化查询 |
| FR-FILTER-03 | 参数联动控件 | 下拉/日期等控件联动刷新 |

---

## 统计报表

### 功能概述

参照 FineReport、Smartbi 的中国式复杂报表能力，补齐分组报表、交叉表、电子表格、填报与订阅推送，形成完整报表体系。

### 报表设计

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-RPT-01 | 报表新建/编辑/删除/复制 | 报表实体与模板管理 |
| FR-RPT-02 | 可视化/源码双模式 | 拖拽设计 + 源码编辑 |
| FR-RPT-03 | 报表数据集绑定 | 绑定数据集与参数 |
| FR-RPT-04 | 报表样式 | 表头、单元格、边框、字体、条件格式 |

### 报表类型

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-RPT-05 | 明细报表 | 逐行明细展示 |
| FR-RPT-06 | 分组报表 | 按维度分组、小计、合计 |
| FR-RPT-07 | 交叉报表 | 行列交叉多维报表 |
| FR-RPT-08 | 电子表格报表 | 类 Excel 的复杂中国式报表 |
| FR-RPT-09 | 图表混合报表 | 报表中嵌入图表 |

### 报表导出

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-RPT-10 | 多格式导出 | Excel、PDF、Word、CSV、图片 |
| FR-RPT-11 | 分页与批量导出 | 分页、批量、定时导出 |

### 定时报表与订阅推送

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-RPT-12 | 定时任务 | 按 cron 定时生成报表 |
| FR-RPT-13 | 订阅推送 | 邮件/站内信推送报表 |
| FR-RPT-14 | 历史报表 | 报表生成历史与归档 |

---

## 系统管理

### 功能概述

提供组织、用户、角色、功能权限、数据权限、登录安全与系统设置的一体化管理，保障多租户、分角色、分数据权限的访问控制。

### 用户与组织管理

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-AUTH-01 | 用户管理 | 用户名、密码、真实姓名、邮箱、管理员标记、启用 |
| FR-AUTH-02 | 组织架构 | 部门/组织树管理，用户归属组织 |
| FR-AUTH-03 | 注册/找回密码 | 可配置禁用；注册用户自动加入默认角色 |

### 角色管理

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-AUTH-04 | 角色管理 | 名称、描述、启用；用户-角色绑定 |
| FR-AUTH-05 | 内置角色 | 注册用户、数据管理员、数据分析员、匿名用户 |
| FR-AUTH-06 | 自定义角色 | 管理员自定义角色与权限组合 |

### 功能权限

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-AUTH-07 | 菜单/功能权限 | 基于角色的菜单与操作权限控制 |
| FR-AUTH-08 | 资源操作权限 | 数据源、项目、数据集、图表、看板、报表的增删改查权限 |

### 数据权限

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-AUTH-09 | 资源级数据权限 | 按「资源 × 主体（全部/角色/用户/匿名）× 权限值（无/读/编辑/删除）」逐条授权 |
| FR-AUTH-10 | 行级数据权限 | 按组织/字段值过滤可见数据行 |
| FR-AUTH-11 | 列级数据权限 | 限制可见字段，敏感列按角色隐藏 |
| FR-AUTH-12 | 默认私有 | 数据默认仅创建者可见，可主动分享 |
| FR-AUTH-13 | 权限值区间 | 无 `[0,20)`、只读 `[20,40)`、编辑 `[40,60)`、删除 `[60,99]`；支持读传递（20/24/28） |

### 登录与安全

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-AUTH-14 | 登录安全 | 登录限流（IP/用户名）、验证码、密码强度校验 |
| FR-AUTH-15 | 密码加密 | `{sha256}`/`{noop}`/`{std}` 前缀，Spring Security 兼容 |
| FR-AUTH-16 | 会话管理 | 登录超时、单点登录/登出 |
| FR-AUTH-17 | 审计日志 | 关键操作审计、登录日志、日志脱敏 |

### 系统设置

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-SYS-01 | 主目录 | `DATAGEAR_HOME`（驱动、插件、看板、数据集文件、临时目录） |
| FR-SYS-02 | 系统数据库 | 内置 Derby 或外置 MySQL |
| FR-SYS-03 | 主题与国际化 | `blue` / `blueDark` 主题、多语言 message 资源 |
| FR-SYS-04 | 通用配置 | 首页欢迎语、CORS、上传大小、缓存、临时目录清理、新版本检测 |

---

## 数据交换及分享

### 功能概述

提供数据导入导出、数据 API 服务、看板/报表分享与订阅推送，实现数据在系统内外的高效流转与共享。

### 数据导入导出

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-EXCH-01 | 数据导入 | CSV/Excel/JSON/SQL 导入（依赖 datagear-dataexchange） |
| FR-EXCH-02 | 数据导出 | 查询结果/数据集导出为多格式 |
| FR-EXCH-03 | 资源导入导出 | 项目（数据集/图表/看板/报表）打包导入导出（规划） |

### 数据 API 服务

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-EXCH-04 | 数据 API 发布 | 将数据集/指标发布为 RESTful API |
| FR-EXCH-05 | API 鉴权 | API Key/Token 鉴权与调用限流 |
| FR-EXCH-06 | API 文档 | 自动生成接口文档与调用示例 |
| FR-EXCH-07 | 开放看板 API | 看板/图表整合至第三方前端（规划） |

### 看板与报表分享

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-EXCH-08 | 分享密码 | 看板分享密码（可匿名访问） |
| FR-EXCH-09 | iframe 嵌入 | 看板/报表 iframe 嵌入第三方系统 |
| FR-EXCH-10 | 链接分享 | 生成分享链接，支持有效期与权限控制 |

### 数据订阅与推送

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-EXCH-11 | 数据订阅 | 订阅数据集/指标，数据更新时通知 |
| FR-EXCH-12 | 推送渠道 | 邮件、站内信、Webhook 推送 |
| FR-EXCH-13 | 定时推送 | 按 cron 定时推送报表/看板快照 |

---

## 综合首页

### 功能概述

参照 Smartbi 门户与观远工作台，打造集数据门户、工作台、全局搜索、收藏与最近访问于一体的综合首页，作为用户进入系统的统一入口。

### 数据门户与工作台

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-HOME-01 | 综合首页 | 数据门户 + 工作台统一展示 |
| FR-HOME-02 | 看板卡片 | 以卡片形式展示常用看板/报表 |
| FR-HOME-03 | 指标卡片 | 核心指标（KPI）卡片展示 |
| FR-HOME-04 | 快捷入口 | 新建数据源/数据集/图表/看板/报表的快捷入口 |
| FR-HOME-05 | 待办与通知 | 待办事项、订阅推送、系统通知聚合 |
| FR-HOME-06 | 首页配置 | 用户自定义首页布局与卡片 |

### 导航与搜索

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-HOME-07 | 全局导航 | 数据接入/治理/分析/展示/报表/管理的一体化导航 |
| FR-HOME-08 | 全局搜索 | 跨数据源/数据集/图表/看板/报表/指标的搜索 |
| FR-HOME-09 | 收藏 | 收藏常用资源，快速访问 |
| FR-HOME-10 | 最近访问 | 最近访问记录，一键直达 |

### 主题与个性化

| 编号 | 功能点 | 说明 |
| --- | --- | --- |
| FR-HOME-11 | 多主题 | 明暗主题切换 |
| FR-HOME-12 | 个性化 | 头像、语言、首页布局、个人设置 |

---

## 非功能需求

### 性能

| 编号 | 需求 | 指标/说明 |
| --- | --- | --- |
| NFR-PERF-01 | 看板/图表响应 | 常规查询亚秒级返回；大屏实时图表支持定时刷新 |
| NFR-PERF-02 | 数据集缓存 | 可配置缓存最大条目（默认 10000）；Caffeine 缓存（默认 500 条/30 分钟） |
| NFR-PERF-03 | 分页查询 | 列表统一分页，避免全量加载 |
| NFR-PERF-04 | SQL 预编译 | `${pc()}` 预编译参数，提升执行效率并防注入 |
| NFR-PERF-05 | 大数据量 | 支持百万级数据集的抽样、聚合与下钻 |

### 安全

| 编号 | 需求 | 说明 |
| --- | --- | --- |
| NFR-SEC-01 | 越权访问校验 | 服务端逐资源校验数据权限 |
| NFR-SEC-02 | SQL 防注入 | 非法关键字校验 + 预编译参数 |
| NFR-SEC-03 | 敏感信息加密 | 数据源密码、看板分享密码可配置加密 |
| NFR-SEC-04 | 日志脱敏 | 日志中敏感信息脱敏处理 |
| NFR-SEC-05 | 登录防护 | IP/用户名登录限流、验证码 |
| NFR-SEC-06 | 数据源防护 | 阻止恶意 URL/连接属性 |
| NFR-SEC-07 | 密码强度 | 可配置正则校验 |
| NFR-SEC-08 | 数据脱敏 | 按角色动态脱敏敏感字段 |

### 可用性与兼容性

| 编号 | 需求 | 说明 |
| --- | --- | --- |
| NFR-AVAIL-01 | 环境兼容 | Java 8+、Servlet 3.1+；内嵌 Tomcat 或外置 WAR |
| NFR-AVAIL-02 | 数据库兼容 | 内置 Derby；可选 MySQL 作为系统库 |
| NFR-AVAIL-03 | 多端适配 | 手机/平板/桌面/大屏适配 |
| NFR-AVAIL-04 | 国际化 | 界面与图表插件标签支持中英文等 |
| NFR-AVAIL-05 | 多主题 | 明暗两套主题 |

### 可扩展性

| 编号 | 需求 | 说明 |
| --- | --- | --- |
| NFR-EXT-01 | 数据源驱动可插拔 | 运行时加载 JDBC 驱动 |
| NFR-EXT-02 | 图表插件化 | 上传 zip 即可扩展图表类型 |
| NFR-EXT-03 | 看板模板开放 | 任意 HTML/JS/CSS + 前端框架 |
| NFR-EXT-04 | 模块化分层 | 九模块清晰分层，各层 API 稳定可替换 |
| NFR-EXT-05 | 权限值留白 | 权限值分段留间隔，便于扩展自定义权限 |
| NFR-EXT-06 | 数据库版本升级 | DDL 版本化自动升级（`DATAGEAR_VERSION`） |

### 可维护性

- 单体应用、轻量部署；日志（SLF4J）；国际化消息外置；配置外置（`application.properties`）。

---

## 关键业务流程

### 数据接入 → 治理 → 展示主流程

```
数据接入（数据库/文件/HTTP/实时流）
   → 数据存储（元数据入库 / 缓存 / 抽取物化）
   → 数据治理（元数据 / 标准 / 质量 / 血缘 / 指标 / 脱敏）
   → 建立数据集（SQL/HTTP/CSV/Excel/JSON，定义字段与参数）
   → 创建图表/报表（选插件，绑定数据集，做数据标记）
   → 组装看板/大屏（HTML 模板 + 图表部件 + 联动/钻取）
   → 分享/嵌入（密码分享、iframe、数据 API）
   → 终端展示（多端响应式、实时刷新、下钻分析）
```

### 数据集查询流程（参数化）

```
看板/图表/报表请求 → 携带参数值（DataSetQuery）
   → 数据集模板解析（Freemarker，${param} / ${pc(param)}）
   → 连接数据源执行（SQL/HTTP/文件读取）
   → 结果结构化（字段映射、类型转换、附加数据）
   → 返回 DataSetResult → 图表/报表渲染
```

### 权限校验流程

```
请求 → 登录态识别（用户/匿名）
   → 功能权限（角色/菜单）
   → 数据权限（Authorization 表：资源 × 主体 × 权限值 + 行列级过滤）
   → 数据脱敏（敏感字段按角色脱敏）
   → 业务执行
```

---

## 版本规划与 Roadmap

### 近期规划

- 综合首页与数据门户、全局搜索、收藏与最近访问。
- 数据治理基础能力：元数据管理、数据字典、数据质量、指标管理。
- 统计报表：分组报表、交叉表、电子表格、报表导出、定时推送。
- GIS 地图下钻、三维立体图表、大数据大屏组件库。
- 数据 API 服务与数据订阅推送。
- 组织架构、行级/列级数据权限、数据脱敏。

### 中期规划

- 数据抽取与数据集市、组合数据集、统计数据集、多维建模。
- 实时/流式数据接入（Kafka、MQTT）与 WebSocket 推送。
- 数据填报、定时邮件报告、项目导入导出。
- 数据血缘可视化与影响分析。
- 提供开放服务 API，便于图表/看板整合至第三方前端。

### 待研究方向

- AI 智能分析（自然语言查询、异常检测、智能洞察）。
- 跨库查询、Docker 安装镜像、云原生部署。
- 多租户隔离与 SaaS 化。

---

## 术语表

| 术语 | 含义 |
| --- | --- |
| 数据源（DtbsSource/Schema） | 注册到系统的外部数据库连接信息 |
| 数据集（DataSet） | 图表/看板/报表消费的统一数据抽象 |
| 图表部件（ChartWidget） | 绑定数据集与插件后的具体图表实例 |
| 图表插件（ChartPlugin） | 定义图表渲染逻辑与数据标记的可插拔单元 |
| 数据标记（DataSign） | 数据集字段到图表语义（名称/数值/类别）的映射 |
| 看板（Dashboard） | 基于 HTML 模板的多图表展示容器 |
| 大屏（Big Screen） | 面向大屏展示的看板形态 |
| 数据权限（Authorization） | 资源 × 主体 × 权限值 的授权 |
| 数据源防护（Guard） | 阻止恶意数据源 URL/连接属性的规则 |
| 文件源（FileSource） | 数据集文件的存储目录 |
| 项目（AnalysisProject） | 数据集/图表/看板/报表的归组单元 |
| 元数据（Metadata） | 描述数据的数据，含技术元数据与业务元数据 |
| 数据血缘（Lineage） | 数据从源头到消费端的流转与依赖关系 |
| 指标（Metric） | 统一口径的业务度量 |
| 数据脱敏（Masking） | 对敏感数据做变形处理以保护隐私 |

---

> 本文档与《SDS-软件设计说明书》配套使用：PRD 定义「做什么」，SDS 定义「怎么做」以及二次开发落地的技术细节。
